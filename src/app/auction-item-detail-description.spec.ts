import { AuctionItemDetailDescription } from './auction-item-detail-description';

describe('AuctionItemDetailDescription', () => {
  it('should create an instance', () => {
    expect(new AuctionItemDetailDescription()).toBeTruthy();
  });
});
