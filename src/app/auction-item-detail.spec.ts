import { AuctionItemDetail } from './auction-item-detail';

describe('AuctionItemDetail', () => {
  it('should create an instance', () => {
    expect(new AuctionItemDetail()).toBeTruthy();
  });
});
